{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}
{% block paragraph %}
  <div class="logo-details__grid {{ content.field_logo_details_grid_class.0 }}">
    {% block content %}
    <div class="grid"><!--
      {% for key, item in content.field_logo_details_grid_item if key|first != '#' %}
    --><div class="grid__item width-tab--1of2 width-lap--1of3 width-desk--1of6">
        <div class="gallery-logos__item {{ key + 1 }}">{{ item }}</div>
      </div><!--
      {% endfor %}
    --></div>
    {% endblock %}
  </div>
{% endblock paragraph %}
